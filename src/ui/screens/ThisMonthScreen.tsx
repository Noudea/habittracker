import { Text, ScrollView } from 'react-native'
import React from 'react'

const ThisMonthScreen = (): JSX.Element => {
  return (
    <ScrollView>
      <Text>This Month Screen</Text>
    </ScrollView>
  )
}

export default ThisMonthScreen
