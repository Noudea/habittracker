import { Text, ScrollView } from 'react-native'
import React from 'react'

const ThisYearScreen = (): JSX.Element => {
  return (
    <ScrollView>
      <Text>This Year Screen</Text>
    </ScrollView>
  )
}

export default ThisYearScreen
